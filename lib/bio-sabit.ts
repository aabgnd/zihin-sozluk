/**
 * Bio için izin verilen en uzun metin. SQL kısıtıyla (015) aynı olmalı.
 *
 * Ayrı ve düz bir modülde: hem sunucu eylemi hem istemci formu kullanıyor.
 * lib/bio.ts sunucu istemcisini içe aktardığı için istemci bileşeni onu
 * import edemez.
 */
export const BIO_MAX = 300;
